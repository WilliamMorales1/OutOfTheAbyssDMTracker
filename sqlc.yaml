version: "2"
sql:
  - engine: "postgresql"
    queries: "db-seeding/queries.sql"
    schema: "db-seeding/schema.sql"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "pgx/v4"
